# Chave da API da Anthropic — usada SOMENTE no servidor (rota /api/laudo).
# Nunca exponha esta chave no cliente nem commite o valor real.
# Copie este arquivo para .env.local e preencha com sua chave.
ANTHROPIC_API_KEY=
